{
  "name": "chrome-search-engine-converter",
  "version": "2.1.0",
  "private": true,
  "description": "Chrome extension to convert searches across multiple engines",
  "scripts": {
    "check:syntax": "node --check extension/engines.js && node --check extension/background.js && node --check extension/popup.js",
    "test": "node --test tests/engines.smoke.test.cjs",
    "check": "npm run check:syntax && npm run test"
  },
  "devDependencies": {
    "sortablejs": "^1.15.6"
  }
}
